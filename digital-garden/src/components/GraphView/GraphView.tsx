import React from 'react';

const GraphView: React.FC = () => {
  return (
    <div className="graph-view">
      <h1>Graph View</h1>
      <p>This will be the interactive graph visualization (similar to Obsidian)</p>
      {/* Will implement graph visualization in future tasks */}
    </div>
  );
};

export default GraphView;